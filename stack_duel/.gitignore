# Dart/Flutter generated
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
*.apk

# IDE / project metadata noise
.idea/
*.iml

# android/ is now COMMITTED (we own the project). The repo-root .gitignore
# ignores android/ globally (for the unrelated RN project), so re-include it here.
!android/
ios/
linux/
macos/
windows/
web/
