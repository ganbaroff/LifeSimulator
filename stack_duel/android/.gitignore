# NOTE: the gradle wrapper (gradlew, gradlew.bat, gradle-wrapper.jar) IS committed
# so CI can build from the committed android/ without regenerating the scaffold.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
