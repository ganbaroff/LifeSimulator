# Критическая проблема: Node 24 несовместим с Expo SDK 50

## Ошибка
```
Error: ENOENT: no such file or directory, mkdir 'node:sea'
```

Это происходит потому что вы используете Node v24.11.0, а Expo SDK 50 поддерживает только Node 18-20 LTS.

## Решение: Установить Node 20 LTS

### Вариант 1: nvm-windows (РЕКОМЕНДУЕТСЯ)

1. **Скачайте nvm-windows:**
   - Перейдите: https://github.com/coreybutler/nvm-windows/releases
   - Скачайте `nvm-setup.exe` (последняя версия)
   - Запустите установщик

2. **После установки откройте НОВОЕ окно PowerShell (от администратора) и выполните:**
   ```powershell
   # Проверка установки
   nvm version
   
   # Установка Node 20 LTS
   nvm install 20
   
   # Переключение на Node 20
   nvm use 20
   
   # Проверка версии
   node --version  # Должно показать v20.x.x
   ```

3. **Перезапустите Metro:**
   ```powershell
   cd C:\Users\yusif\OneDrive\Desktop\LifeSimulator
   npx expo start -c
   ```

### Вариант 2: Переменная окружения (ВРЕМЕННОЕ РЕШЕНИЕ)

Если не можете установить nvm прямо сейчас, попробуйте:

```powershell
# Установите переменную окружения для текущей сессии
$env:EXPO_NO_METRO_EXTERNALS="1"

# Запустите Metro
npx expo start -c
```

⚠️ **Внимание:** Это временное решение. Для стабильной работы нужен Node 20 LTS.

### Вариант 3: Переустановка Node

1. Удалите текущий Node 24 через "Установка и удаление программ"
2. Скачайте Node 20 LTS отсюда: https://nodejs.org/en/download
3. Установите Node 20 LTS
4. Перезапустите PowerShell

## После исправления

Когда Node 20 будет установлен:

```powershell
# Проверьте версию
node --version  # v20.x.x

# Очистите зависимости
Remove-Item node_modules -Recurse -Force
npm install

# Запустите проект
npx expo start -c
```

## Почему это важно?

- Node 24 - слишком новая версия для текущего стека
- Expo SDK 50 тестировался с Node 18-20
- Без исправления приложение не запустится
- Это блокирует любую разработку и тестирование

## Статус проекта

✅ **Готово:**
- TypeScript миграция завершена
- Переменные окружения настроены
- Все сервисы и экраны мигрированы
- 0 ошибок TypeScript

🔴 **Блокер:**
- Node 24 → нужен Node 20 LTS

После исправления Node можно запускать приложение на Android/iOS!
